# auto_download.yml
name: Auto download

on:
  workflow_dispatch:
  schedule:
    - cron: '0 18 * * *'
jobs:
  auto-download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: get current id
        run: echo "CURRENTID=$(awk '{print}' current_id)" >> $GITHUB_ENV
        
      # - uses: convictional/trigger-workflow-and-wait@v1.6.1
      #   with:
      #     owner: keithconvictional
      #     repo: myrepo
      #     github_token: ${{secrets.PERSONAL_TOKEN}}
      #     github_user: github-user
      #     workflow_file_name: download_dispatch.yml
      #     ref: master
      #     wait_interval: 10
      #     client_payload: '{"JM_ALBUM_IDS":"${{ env.CURRENTID }}","IMAGE_SUFFIX":"png","ZIP_NAME":"${{ env.CURRENTID }}.zip"}'
      #     propagate_failure: false
      #     trigger_workflow: true
      #     wait_workflow: true
      - name: TRIGGER WORKFLOW
        run: |
              echo '{"JM_ALBUM_IDS":"${{ env.CURRENTID }}", "IMAGE_SUFFIX":"png", "ZIP_NAME":"${{ env.CURRENTID }}.zip"}' | gh workflow run download_dispatch.yml --json
              gh workflow run 88013593 -f JM_ALBUM_IDS=${{ env.CURRENTID }} 
      # - name: trigger download workflow
      #   run: |
      #         gh api \
      #         --method PUT \
      #         -H "Accept: application/vnd.github+json" \
      #         -H "X-GitHub-Api-Version: 2022-11-28" \
      #         /repos/GodLufei/JMComic-Crawler-Python/actions/workflows/88013593/enable
      #         curl -L -H "Accept: application/vnd.github+json" -H "Authorization: Bearer ${{secrets.PERSONAL_TOKEN}}" https://api.github.com/repos/GodLufei/JMComic-Crawler-Python/actions/workflows/88013593 -d "{\"JM_ALBUM_IDS\":\"${{ env.CURRENTID }}\",\"IMAGE_SUFFIX\":\"png\",\"ZIP_NAME\":\"${{ env.CURRENTID }}.zip\"}"

      - name: change current id
        run: current_id=$(awk '{sum = $0+1; print sum}' current_id) && echo $current_id > current_id 

      - name: Commit files
        run: |
              git config --local user.email "370933147@qq.com"
              git config --local user.name "GodLufei"
              git commit -a -m "update current_id"
          
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.PERSONAL_TOKEN }}
          branch: ${{ github.ref }}


      # - name: git push 
      #   run: git add . && git commit -m 'update current_id' && git push origin head
